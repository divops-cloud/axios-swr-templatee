echo "# divops-app" >> README.md &&
git init && 
git add . && 
git commit -m "1" && 
git branch -M main && 
git remote add origin git@github.com:divopsnetwork/divops-app.git &&
git push -u origin main